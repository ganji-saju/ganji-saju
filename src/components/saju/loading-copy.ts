// 2026-08-30 #716 — 사주 제출 → 결과 사이에 **로딩이 두 번** 보이던 것.
//
//   제보: "로딩화면이 나왔다가 페이지 전환이 되고 다시 또 로딩화면이 나오고나서 풀이가
//   나와. 페이지 이동을 두 번 하는 건지 로딩을 두 번 부르는 건지… 전부터 계속 거슬렸어."
//
//   2026-08-30 진단: "대기가 둘(POST → 서버 렌더)이라 합칠 수 없다, 문구만 맞추자" — **틀렸다.**
//   2026-08-31 재제보("안 고쳐졌어") 후 확정: 두 번째 화면은 /saju/[slug]/loading.tsx 였다.
//   제출 오버레이는 router.push 뒤에도 남는데(saju-new-client 가 submitting 을 안 끈다), Next 가
//   이동 대상 세그먼트의 loading.tsx 를 **즉시** 폴백으로 갈아끼워 오버레이가 사라지고 같은 문구의
//   두 번째 화면이 떴다. loading.tsx 를 지우면 오버레이가 결과가 올 때까지 남는다 → 로딩 한 번.
//   (직접 새로고침 땐 브라우저 스피너만 보인다 — 첫 화면 전 DB 호출은 병렬로 줄였다.)
//
//   이 문구는 이제 /saju/new 제출 오버레이 한 곳에서만 쓴다. print/loading.tsx 는 별개(PDF 흐름).
export const SAJU_RESULT_LOADING = {
  title: '사주를 풀어드리고 있어요',
  description: '네 기둥(年月日時)을 정리하고 오늘 흐름과 맞춰보는 중입니다.',
  steps: ['사주 4기둥 정리', '오행 흐름 분석', '격국·용신 도출', '오늘 운 매칭'],
} as const;
