// 2026-08-30 #716 — 사주 제출 → 결과 사이에 **로딩이 두 번** 보이던 것.
//
//   제보: "로딩화면이 나왔다가 페이지 전환이 되고 다시 또 로딩화면이 나오고나서 풀이가
//   나와. 페이지 이동을 두 번 하는 건지 로딩을 두 번 부르는 건지… 전부터 계속 거슬렸어."
//
//   확인 결과 **페이지 이동은 한 번**이고, 로딩이 서로 다른 두 곳에서 뜬다:
//     1) /saju/new 에서 제출하면 `submitting` 이 true → 클라이언트 오버레이(POST 대기)
//     2) router.push 로 /saju/[slug] 로 가면 Next 의 loading.tsx (서버 렌더 대기)
//   둘은 **원래 다른 대기**라 하나로 합칠 수 없다. 하지만 **다른 화면처럼 보일 이유는 없다** —
//   문구가 달라서 "또 떴다" 로 읽혔다. 같은 문구를 쓰면 한 번의 로딩으로 이어져 보인다.
//
//   ⚠️ 두 곳이 각자 문구를 들고 있으면 다시 갈린다. 여기서만 바꿔라.
export const SAJU_RESULT_LOADING = {
  title: '사주를 풀어드리고 있어요',
  description: '네 기둥(年月日時)을 정리하고 오늘 흐름과 맞춰보는 중입니다.',
  steps: ['사주 4기둥 정리', '오행 흐름 분석', '격국·용신 도출', '오늘 운 매칭'],
} as const;
